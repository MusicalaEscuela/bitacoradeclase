{
  "name": "musicala-rip-status-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Publica el estado calculado por RIP (studentComputed) hacia bitacoras-de-clase.",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "node --check index.js",
    "test": "node test/permissions.test.js",
    "deploy": "firebase deploy --only functions --project rip-musicala"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.1"
  }
}
